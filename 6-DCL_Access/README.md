
#### GRANT & REVOKE

The SQL commands GRANT and REVOKE are the two primary statements in DCL (Data Control Language). 
They are used by database administrators to manage user permissions, controlling who can access and manipulate database objects.

